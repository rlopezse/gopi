name: CI - Self Hosted Runner

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Ver info del sistema
        run: |
          uname -a
          node -v || true
          npm -v || true

      - name: Ejecutar script
        run: |
          echo "Runner ejecutándose en la Raspberry 🚀"
